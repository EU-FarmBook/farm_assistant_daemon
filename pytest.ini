[pytest]
asyncio_mode = auto
# Collect only this project's tests. hermes-data/ is the agent's live volume:
# once the stack has run, it holds the agent's own bundled skills — with their
# own test suites, which fail here and have nothing to do with this service.
testpaths = tests
